import { FiltercolourPipe } from './filtercolour.pipe';

describe('FiltercolourPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltercolourPipe();
    expect(pipe).toBeTruthy();
  });
});
