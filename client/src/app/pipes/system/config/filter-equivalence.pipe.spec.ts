import { FilterEquivalencePipe } from './filter-equivalence.pipe';

describe('FilterEquivalencePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEquivalencePipe();
    expect(pipe).toBeTruthy();
  });
});
