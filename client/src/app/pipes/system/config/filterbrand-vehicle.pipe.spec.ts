import { FilterbrandVehiclePipe } from './filterbrand-vehicle.pipe';

describe('FilterbrandVehiclePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterbrandVehiclePipe();
    expect(pipe).toBeTruthy();
  });
});
