import { FiltermodelVehiclePipe } from './filtermodel-vehicle.pipe';

describe('FiltermodelVehiclePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltermodelVehiclePipe();
    expect(pipe).toBeTruthy();
  });
});
