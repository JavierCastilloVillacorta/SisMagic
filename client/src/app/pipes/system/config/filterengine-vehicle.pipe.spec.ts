import { FilterengineVehiclePipe } from './filterengine-vehicle.pipe';

describe('FilterengineVehiclePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterengineVehiclePipe();
    expect(pipe).toBeTruthy();
  });
});
