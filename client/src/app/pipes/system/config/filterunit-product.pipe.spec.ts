import { FilterunitProductPipe } from './filterunit-product.pipe';

describe('FilterunitProductPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterunitProductPipe();
    expect(pipe).toBeTruthy();
  });
});
