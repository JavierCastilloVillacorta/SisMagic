import { FilterbrandProductPipe } from './filterbrand-product.pipe';

describe('FilterbrandProductPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterbrandProductPipe();
    expect(pipe).toBeTruthy();
  });
});
