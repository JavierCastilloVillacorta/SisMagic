import { FilterpositionProductPipe } from './filterposition-product.pipe';

describe('FilterpositionProductPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterpositionProductPipe();
    expect(pipe).toBeTruthy();
  });
});
