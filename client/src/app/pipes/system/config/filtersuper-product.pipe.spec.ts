import { FiltersuperProductPipe } from './filtersuper-product.pipe';

describe('FiltersuperProductPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltersuperProductPipe();
    expect(pipe).toBeTruthy();
  });
});
